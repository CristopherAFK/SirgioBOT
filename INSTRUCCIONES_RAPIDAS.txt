╔══════════════════════════════════════════════════════════════╗
║          INSTRUCCIONES RÁPIDAS PARA RENDER                   ║
╔══════════════════════════════════════════════════════════════╗

✅ CAMBIOS COMPLETADOS EN EL CÓDIGO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- index.js reescrito con flujo correcto de inicio
- Servidor web agregado para Health Check de Render
- Validación de token mejorada
- Módulos se cargan DESPUÉS de conectar


📋 PASOS PARA DESPLEGAR EN RENDER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. SUBIR CAMBIOS A GITHUB
   ────────────────────────────────────────────────────────────
   git add .
   git commit -m "Fix: Corregir conexión del bot en Render"
   git push origin main


2. CONFIGURAR VARIABLES EN RENDER
   ────────────────────────────────────────────────────────────
   Dashboard → Tu Servicio → Environment → Add Environment Variable
   
   Variable 1:
   Key:   DISCORD_TOKEN
   Value: [Tu token del bot de Discord]
   
   Variable 2 (opcional pero recomendado):
   Key:   MONGODB_URI
   Value: [Tu URI de MongoDB Atlas]


3. VERIFICAR CONFIGURACIÓN DEL SERVICIO
   ────────────────────────────────────────────────────────────
   Build Command:      npm install
   Start Command:      node index.js
   Health Check Path:  /health


4. ACTIVAR INTENTS EN DISCORD DEVELOPER PORTAL
   ────────────────────────────────────────────────────────────
   URL: https://discord.com/developers/applications
   
   Pasos:
   a) Selecciona tu aplicación
   b) Ve a la sección "Bot"
   c) Baja hasta "Privileged Gateway Intents"
   d) Activa estos 3 intents:
      ☑ PRESENCE INTENT
      ☑ SERVER MEMBERS INTENT
      ☑ MESSAGE CONTENT INTENT
   e) Guarda los cambios


5. DESPLEGAR Y VERIFICAR
   ────────────────────────────────────────────────────────────
   Render desplegará automáticamente después del push.
   
   En los logs deberías ver:
   🌐 Servidor web activo en puerto 5000
   🚀 Iniciando SirgioBOT...
   ✅ Token validado
   🔌 Intentando conectar a Discord...
   ✅ Login exitoso
   ✅ ¡Bot listo! Conectado como SirgioBOT#XXXX


🔧 SOLUCIÓN DE PROBLEMAS COMUNES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Error: "Token no válido"
→ Verifica que copiaste el token completo sin espacios
→ El token debe tener ~70 caracteres

Error: "Privileged intent provided but not enabled"
→ Activa los intents en Discord Developer Portal (paso 4)

Error: "Health check failed"
→ Verifica que Start Command sea: node index.js
→ Verifica que Health Check Path sea: /health

Error: "ECONNREFUSED" o "MongoDB connection failed"
→ Verifica tu MONGODB_URI
→ El bot funcionará sin MongoDB pero sin persistencia


📞 OBTENER TU TOKEN DE DISCORD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Ve a: https://discord.com/developers/applications
2. Selecciona tu aplicación (o crea una nueva)
3. Ve a la sección "Bot"
4. Haz clic en "Reset Token" o "Copy" si ya tienes uno
5. Copia el token completo
6. Pégalo en Render como variable DISCORD_TOKEN


🧪 PRUEBA LOCAL (OPCIONAL)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Antes de desplegar, puedes probar localmente:

1. Crea archivo .env:
   echo "DISCORD_TOKEN=tu_token_aqui" > .env

2. Instala dependencias:
   npm install

3. Ejecuta test de conexión:
   node test-connection.js

4. Si pasa el test, ejecuta el bot:
   node index.js


✨ ARCHIVOS DE REFERENCIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- SOLUCION_RENDER.md    → Explicación detallada de los cambios
- RESUMEN_CAMBIOS.md    → Resumen ejecutivo
- test-connection.js    → Script para probar la conexión
- index.js              → Archivo principal corregido


═══════════════════════════════════════════════════════════════
¡LISTO! Después de seguir estos pasos, tu bot debería conectar
correctamente en Render. Si tienes problemas, revisa los logs
completos en el Dashboard de Render.
═══════════════════════════════════════════════════════════════
